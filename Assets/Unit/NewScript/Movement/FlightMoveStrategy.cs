using UnityEngine;

/// <summary>
/// 飞行沿路径移动策略
/// 继承 BaseMoveStrategy，无额外特殊行为。
/// 飞行单位不旋转朝向。
/// </summary>
public class FlightMoveStrategy : BaseMoveStrategy
{
    // 飞行单位当前无特殊行为，全部逻辑在 BaseMoveStrategy 中
}
